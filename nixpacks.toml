# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build",
  "npm run build:server",
  "npm run db:generate"
]

[start]
cmd = "npm start"
